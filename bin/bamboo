#!/usr/bin/env perl6
use v6;

use Bamboo;

multi sub MAIN('install', :$path = '.') {
    my $bamboo = Bamboo.new(:$path);
    $bamboo.install;
}

multi sub MAIN('init') {
    # TODO : generate META.info
    die "NYI";
}
